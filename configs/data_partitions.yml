# Data partition lock — §C.9.
# These ranges become binding once filled and committed. Edits require a
# Change Request. Until then the file holds placeholders that the
# contamination guard treats as "not yet locked": any attempt to query the
# `validation` partition while the lock is missing is rejected.
#
# Signer identifier `DIR-01` is the pseudonymous Director-Sponsor ID per
# `docs/OWNERS.md`. Ranges promoted from `configs/data_partitions.draft.yml`.
# Per errata §6 / §B.13, registry-first registration governs v0.3 OOS reuse;
# this lock binds v0.2.

locked: true
locked_by: "DIR-01"
locked_at_iso: "2026-05-09T10:00:00-04:00"

partitions:
  training:
    start: "2019-05-06"     # MES launch.
    end:   "2024-12-31"
  validation:
    start: "2025-01-02"     # first MES session of 2025.
    end:   "2025-12-31"
  final_holdback:
    start: "2026-01-02"
    end:   "2026-04-30"
