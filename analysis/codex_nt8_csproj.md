I'm stuck fixing a NinjaTrader 8 NinjaScript compile error and need an independent eye. Don't be polite — push back on anything I've done wrong.

## Setup
NinjaTrader 8 (8.1.6.3) running on Parallels-on-macOS. User Documents folder is on the Mac side: `\\Mac\Home\Documents\NinjaTrader 8\`. NT8's NinjaScript editor invokes MSBuild on `bin\Custom\NinjaTrader.Custom.csproj` (SDK-style, net48). The project has Resource.resx + 8 locale-specific .resx files (de-DE, es-ES, fr-FR, it-IT, ko-KR, pt-PT, ru-RU, zh-Hans).

## Original symptom
NT8's NinjaScript Editor → F5 produced 64 CS0579 "Duplicate '...' attribute" errors. Pattern: 8 attribute types (AssemblyCompany, AssemblyCopyright, AssemblyFileVersion, AssemblyInformationalVersion, AssemblyProduct, AssemblyTitle, AssemblyVersion, AssemblyCulture) × 8 occurrences each = 64.

## What I've tried so far

**Step 1:** Found a stale `bin\Custom\AssemblyInfo.cs` referencing NT8 8.1.7.0 attributes (NT8 install is 8.1.6.3). Moved it to a backup folder. No change.

**Step 2:** Looked at `NinjaTrader.Custom.csproj` and found 8 explicit `<Compile Include="obj\Release\{locale}\NinjaTrader.Custom.resources.cs" />` entries (zh-Hans, fr-FR, de-DE, es-ES, ru-RU, pt-PT, ko-KR, it-IT). Each of those generated files defines all 8 assembly attributes (sample below). Removed those 8 lines from the csproj.

**Sample auto-gen file** (`obj\Release\zh-Hans\NinjaTrader.Custom.resources.cs`):
```csharp
//------------------------------------------------------------------------------
// <auto-generated>... Generated by the MSBuild WriteCodeFragment class.
[assembly: System.Reflection.AssemblyCompanyAttribute("NinjaTrader")]
[assembly: System.Reflection.AssemblyCopyrightAttribute("NinjaTrader© 2003-2023 NinjaTrader LLC")]
[assembly: System.Reflection.AssemblyFileVersionAttribute("8.1.7.0")]
[assembly: System.Reflection.AssemblyInformationalVersionAttribute("8.1.7.0")]
[assembly: System.Reflection.AssemblyProductAttribute("NinjaTrader")]
[assembly: System.Reflection.AssemblyTitleAttribute("NinjaTrader Custom")]
[assembly: System.Reflection.AssemblyVersionAttribute("8.1.7.0")]
[assembly: System.Reflection.AssemblyCultureAttribute("zh-Hans")]
```

(Note that the auto-gen says 8.1.7.0 but the running NT8 is 8.1.6.3 — same mismatch as the dead AssemblyInfo.cs had. May be a clue.)

## Current state after both fixes
F5 now reports only 6 CS0579 errors, all on `NinjaTrader\Custom\resources.cs`:
```
Duplicate 'System.Reflection.AssemblyCompanyAttribute' attribute        line 13
Duplicate 'System.Reflection.AssemblyCopyrightAttribute' attribute      line 14
Duplicate 'System.Reflection.AssemblyFileVersionAttribute' attribute    line 15
Duplicate 'System.Reflection.AssemblyInformationalVersionAttribute' attribute  line 16
Duplicate 'System.Reflection.AssemblyProductAttribute' attribute        line 17
Duplicate 'System.Reflection.AssemblyTitleAttribute' attribute          line 18
```
The 2 attributes that DISAPPEARED from the error list: AssemblyVersionAttribute (line 19) and AssemblyCultureAttribute (line 20).

The error path says `NinjaTrader\Custom\resources.cs` (no obj/, no locale folder, no "NinjaTrader.Custom." prefix). I cannot find such a literal file on disk — the only files with `[assembly:` attributes are still the 8 `obj\Release\{locale}\NinjaTrader.Custom.resources.cs` files I left on disk (just removed from csproj).

## Relevant csproj properties
```xml
<EnableDefaultCompileItems>false</EnableDefaultCompileItems>
<GenerateAssemblyInfo>false</GenerateAssemblyInfo>
<LangVersion>13.0</LangVersion>
<TargetFramework>net48</TargetFramework>
<UseWPF>true</UseWPF>
```

EmbeddedResource entries (unchanged):
```xml
<EmbeddedResource Update="Resource.resx">
  <Generator>PublicResXFileCodeGenerator</Generator>
  <LastGenOutput>Resource.Designer.cs</LastGenOutput>
  <CustomToolNamespace>NinjaTrader.Custom</CustomToolNamespace>
  <SubType>Designer</SubType>
</EmbeddedResource>
<EmbeddedResource Update="Resource.ru-RU.resx"><DependentUpon>Resource.resx</DependentUpon></EmbeddedResource>
<EmbeddedResource Update="Resource.fr-FR.resx"><DependentUpon>Resource.resx</DependentUpon></EmbeddedResource>
... (8 more locale .resx update entries)
```

And:
```xml
<ItemGroup>
  <None Remove="obj\**" />
  <Page Remove="obj\**" />
</ItemGroup>
```

## Filesystem state right now
- `bin\Custom\Resource.resx` and 8 locale `.resx` files: present, untouched.
- `bin\Custom\Resource.Designer.cs`: present (auto-generated, references `Resource.resx`).
- `bin\Custom\obj\Release\{locale}\NinjaTrader.Custom.resources.cs` (×8): still on disk, no longer referenced by csproj.
- `bin\Custom\obj\Release\NinjaTrader.Custom.dll`: present (currently locked by running NT8).
- `bin\Custom\obj\Release\.NETFramework,Version=v4.8.AssemblyAttributes.cs`: still referenced in csproj.

## Constraints
- NT8 is running, so `bin\Custom\NinjaTrader.Custom.dll` is locked. I can't blow away `obj\` clean.
- Auto-mode permission denied "rm -rf" of the locale subfolders earlier.
- I cannot ask the user to close NT8 unless absolutely necessary — they're already frustrated.

## Questions
1. **Where is `NinjaTrader\Custom\resources.cs` (the singular, non-locale "resources.cs" the error points to)?** Is this the MSBuild target generating attribute-fragment files for the *neutral* culture, separate from the satellite locale files? Or is it `Resource.Designer.cs` masquerading under a different name in NT8's editor display?
2. **Why did 6 of 8 duplicates persist and 2 (AssemblyVersion, AssemblyCulture) disappear** after I removed the 8 locale-specific Compile entries? That asymmetry is the most diagnostic clue, but I don't see why.
3. **What's the minimal fix that doesn't require closing NT8?** Ideally a csproj edit + maybe a file remove, no clean rebuild.
4. Am I patching the wrong thing entirely? Should I be looking at NT8's NinjaScript editor settings, the .targets files it imports, or a stale `*.cache` file in `obj\Release\` instead of the csproj itself?

Repo + the deployed files for inspection:
- Live csproj: `\\Mac\Home\Documents\NinjaTrader 8\bin\Custom\NinjaTrader.Custom.csproj`
- Backup of original csproj: `\\Mac\Home\Documents\NinjaTrader 8\backup-2026-05-10\NinjaTrader.Custom.csproj`
- The 8 stray locale auto-gens: `\\Mac\Home\Documents\NinjaTrader 8\bin\Custom\obj\Release\{locale}\NinjaTrader.Custom.resources.cs`

Keep it tight. Bullet points, no preamble. If I'm wrong about the diagnosis, say so first.
