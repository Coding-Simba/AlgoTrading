# v1.4-r1 Errata & Sprint Kick-off Note

**Status:** Administrative clarification and sprint kick-off. Not a Change Request. Not a spec content change.
**Applies to:** Build Plan v1.4-r1 (Sign-Off Candidate)
**Issued:** 2026-05-09
**Owner:** Director Sponsor

---

## 1. Document references

All references to "v1.4" in correspondence, tickets, the strategy registry, and tooling are to be read as "v1.4-r1." There is no separate v1.4 in circulation.

## 2. Repository branch

Spec branch name: `release/signoff-v1.4-r1`. In this environment, harness
policy mandates development on `claude/release-signoff-v1.4-r1-RfVcm`. Promote
to the spec-named branch and tag `v1.4-r1` once all pre-code sign-off rows are
signed.

## 3. Freeze scope (clarification)

The FROZEN status applies to the **pre-code sign-off rows only**:

- Appendix B
- Appendix C
- Appendix D *(appendix approval — placeholders permitted for framework work)*
- Appendix E
- Appendix H

Subsequent phase gates — broker rate sheet substitution, Appendix F finalization, Appendix I counsel sign-off, validation freeze, paper-trading transition, small-size live transition, Appendix G, scale-up — are signed off as the program reaches each phase. Their unsigned status does **not** indicate the document is unfrozen and does **not** block framework engineering.

## 4. Appendix I gating (clarification)

- Required **before paper or live trading**.
- **Not** required before v0.2 strategy code, **provided** the capital base is internal-proprietary only.
- If external or client capital is in scope at any point before strategy code begins, Appendix I sign-off becomes a coding prerequisite. Flag immediately to counsel and to the Director Sponsor.

## 5. Broker rate sheet gating (clarification)

- Required **before any approved v0.2 strategy backtest, validation, or OOS run**.
- **Not** required before framework engineering or fill-model unit tests.
- Placeholder costs in §D.2 are permitted for unit-test scaffolding only and **must not** appear in any approved backtest output, research report, or pass/fail evaluation.

## 6. §C.9 and §B.13 alignment

For any v0.3 OOS/holdback question, the controlling rule is **§B.13**:

- The v0.2 OOS partition may serve as clean validation for v0.3 **only if** v0.3 rules were registered in the strategy registry **before** the v0.2 OOS query was run.
- Otherwise, the v0.2 OOS partition is contaminated for v0.3 and may be used as a diagnostic comparison only — never as a clean pass/fail gate.
- The final holdback partition is binding family-level validation regardless.
- Any v0.3 designed in response to v0.2 OOS results must disclose the contamination explicitly in the research report.

§C.9 wording defers to §B.13 if any apparent inconsistency arises; the registry rule in §B.13 is canonical.

---

## 7. Owners

See `docs/OWNERS.md`. No unnamed workstreams. No multi-role single ownership
for risk reviewer (independence per v1.0).

## 8. Procurement tasks (parallel, Day 0)

See `docs/PROCUREMENT.md` for the live checklist.

## 9. Sprint 1 build order (framework only — no v0.2 logic)

1. Repository structure + strategy registry
2. Data ingestion skeleton
3. Session calendar + holiday / half-day handling
4. Tick + BBO parser
5. 5m and 60m bar builder with boundary tests (per §B.5)
6. Fill model unit tests (per Appendix D, including stop-trigger and intrabar collision)
7. Order-state-machine simulator (per §H.2)
8. Baseline runner skeletons (random, drift-matched random, reversed signal, session-exposure)
9. Latency / clock-drift monitoring scaffolding
10. Contamination-log tooling and enforcement

No v0.2 strategy code in this sprint.

## 10. Gate summary

See `docs/GATES.md`.

---

**Decision:** Framework work proceeds now. Stop editing the spec unless a
formal Change Request is raised. Next deliverable is engineering output
(Sprint 1 commits), not another document revision.

*End of errata note.*
